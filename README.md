# Neuromorphic Logic Controller

A simulation environment where a virtual agent (e.g., a drone in Gazebo or a custom Python sim) is controlled by an event-stream-processing "brain" implemented as a Finite State Machine (FSM) in SystemVerilog.
